{
  "name": "mastra-tui",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "dev:server": "turbo dev --filter=@mastra-tui/server",
    "dev:terminal": "turbo dev --filter=@mastra-tui/terminal"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "bun@1.2.4",
  "dependencies": {
    "@mastra/ai-sdk": "^1.0.3"
  }
}
